<% @comments.each do |comment| %>
<div class="media">
  <small>
    <b><%= comment.user.name %>'s 2¢:</b>
  </small>
  <small>
    <% if policy(comment).destroy? %>
      | <%= link_to "Delete", [@topic, @post, comment], method: :delete %>
    <% end %>
  </small>
  <p>
    <%= comment.body %>
  </p>
</div>

<% end %>
